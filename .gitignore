# secrets — never commit
.env
.env.local
*.key

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# os
.DS_Store

# transient run scratch (raw transcripts under runs/ ARE committed — see docs/notes.md)
runs/**/_scratch/
node_modules/

# procedurally generated noise beds — rebuild with `python -m harness.tts noise`
fixtures/audio/noise_*.wav

# vendored browser SDK bundles — fetched on first voice run
harness/channels/page/vendor/
